<script lang="ts">
  export let lib: string;
  if (lib == '@sinclair/typebox') lib = 'typebox';
  else if (lib == '@vinejs/vine') lib = 'vine';
</script>

<p>
  Alternatively, <a
    href="https://sveltelab.dev/github.com/ciscoheat/superforms-examples/tree/tutorial-{lib}"
    target="_blank">open the tutorial on SvelteLab</a> to follow along in the browser and copy the code
  from there.
</p>
